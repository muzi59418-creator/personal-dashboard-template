# 更新日志

## v1.3.0 - 2026-07-07

- 项目模块命名升级：用户可见文案统一为“项目推进清单 / 推进事项”。
- 项目进度改为按推进事项完成情况自动计算；无推进事项项目显示“未拆分推进事项”，并从平均进度中排除。
- 推进事项新增“创建工作”，项目下一步动作新增“转为工作”，两者都会先打开新增工作弹窗，由用户确认后才保存。
- 工作内容新增项目来源元数据，可追溯来源项目、来源推进事项或项目下一步动作。
- 来源于推进事项的工作完成时，支持用户确认后同步标记推进事项完成；不会自动改变整个项目状态。
- 旧手动进度字段继续保留，旧 JSON 备份仍可导入，新导出继续包含 `version`、`appVersion` 与 `schemaVersion`。

## v1.2.0 - 2026-07-07

- 例行工作规则新增生效日期、结束日期、暂停、暂停至日期、恢复方式和每天频率节假日策略。
- 新增离线维护的 2026 年中国法定工作日历，按国务院办公厅 2026 年节假日安排配置法定节假日和调休上班日。
- 自动生成保留“按规则日期检查”的机制，并在应用初始化、窗口重新获得焦点、日期切换和手动同步时执行检查。
- 法定节假日顺延支持连续假期同规则最多补一次，节后补办和同日正常触发会合并为 1 条任务。
- 单次例行工作新增顺延、本次跳过、暂停规则、查看规则和顺延追溯信息。
- 读取旧 localStorage 时先保存 v1.2.0 迁移前备份；旧 JSON 备份仍可导入，新导出继续包含 `appVersion` 与 `schemaVersion`。

## v1.1.0 - 2026-07-07

- 今日工作升级为今日工作台，拆分优先处理、今日例行、项目推进、今日已完成。
- 工作内容新增计划执行日，筛选、编辑、详情、导入导出均兼容。
- 新增工作模板入口和模板管理能力。
- 导出 JSON 增加 `appVersion` 与 `schemaVersion`，旧 JSON 备份仍可导入。

## v1.0.0

- 将既有本地工作内容、日记、想法、项目、例行工作、备份导入导出能力统一作为 1.0.0 基线。
